#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "grafo.h"

void initialize_search(grafo *g);
void process_vertex(int v);
void process_edge(int x, int y);
bool valid_edge(int e);
void bfs(grafo *g, int start);
void dfs(grafo *g, int v);
void find_path(int start, int end, int parent[]);